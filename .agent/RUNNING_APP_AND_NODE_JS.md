Add node and npm to the path. They are installed in /opt/homebrew/bin.
Test server starts on port 8080.
When testing in browser beware of the fact that demo is cached by service worker so you need to clear the cache to see the changes.